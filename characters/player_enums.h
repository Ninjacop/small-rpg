// player_enums.h
// Teegan Lutze
#ifndef PLAYER_ENUMS_H
#define PLAYER_ENUMS_H

// Enum to store all possible elements
enum Elements {
  WATER = 0,
  GRASS, 
  FIRE,
  NONE,
  ELEMENT_UNBOUND
};

// Enum to store all creature types
enum Creatures {
  PLAYER = 0,
  ENEMY
};  

#endif